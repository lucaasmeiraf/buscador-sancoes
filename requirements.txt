# Dependências dos scripts de coleta/envio
requests
pypdf
# A VM do ambiente de nuvem traz uma cryptography de sistema quebrada (binding
# Rust sem _cffi_backend) que derruba o import do pypdf com PanicException.
# Só instala uma versão íntegra com --upgrade (sem ele o pip dá "already
# satisfied" na quebrada e não faz nada). coleta_dodf.py também se protege
# sozinho, neutralizando o pacote podre antes do import do pypdf.
cryptography
