# Segredos — nunca commitar
.env

# Artefatos de execução. Versionados: data/vistos.json (estado de dedup) e
# data/leads.csv (planilha-mestre — é o histórico, tem de sobreviver ao clone).
data/raw/
data/candidatos.json
data/candidatos/
data/leads_hoje.json
data/resumo.txt
# Blocos do DODF trazidos da branch `dados/dodf` — vivem lá, não na master.
data/dodf/

# Documento de escopo (briefing de trabalho, mantido fora do repositório)
buscador_sancoes.docx

# Python
__pycache__/
*.pyc
.venv/
